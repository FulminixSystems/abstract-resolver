# abstract-resolver
A zero-dependency, multi-language structural repair engine in Rust. Sub-microsecond O(N) coordinate boundary tracking inside a &lt;138KB standalone footprint with 0 heap allocations.
